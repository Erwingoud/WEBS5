meta {
  name: Delete Own Upload
  type: http
  seq: 14
}

delete {
  url: {{host}}:{{gateway_port}}/targets/{{target_id}}/participants/participant1
  auth: none
}

headers {
  Authorization: Bearer {{participant_jwt}}
}